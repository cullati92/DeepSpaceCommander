/*
using UnityEngine;
using System.Collections;

public class shipController : MonoBehaviour {

	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
		while (Touch.position.x > 0 && Touch.position.y < 20 && this.position.x <50) {
			//Move right
			this.position.x ++
		}
		while (Touch.position.x < 0 && Touch.position.y < 20 && this.position.x <-50) {
			//Move left
			this.position.x --
		}
		while (Touch.position.y > 20{
			//shoot projectile
		}
	}
}
*/